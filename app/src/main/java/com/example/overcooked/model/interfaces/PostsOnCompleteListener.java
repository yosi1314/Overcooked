package com.example.overcooked.model.interfaces;

import com.example.overcooked.model.Post;

import java.util.List;

public interface PostsOnCompleteListener {
    void onComplete(List<Post> posts);
}
